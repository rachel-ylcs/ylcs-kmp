在创建`Application`时需要提供一个平台上下文环境，它通常包含了平台需要的特定值。

当你需要编写平台特定代码时，需要从这个上下文环境里获取到相关信息。

例如在`Android`上`android.content.Context`和`Activity`等都是可能在`Android`开发中使用到的，
还有在桌面端的窗口句柄可能在原生窗口操作时用到的等等。

你可以直接引用全局的`app.context`来获取平台上下文环境，并引用其中相关信息。

!!! Tip
    Context是一个expect类，你在公共代码处无法知晓其平台，应该在具体平台侧代码使用。